services:
  directus:
    image: directus/directus:12.0.0-rc.1
    restart: unless-stopped
    ports:
      - "8056:8056"
    volumes:
      - ./directus/database:/directus/database
      - ./directus/uploads:/directus/uploads
      - ./directus/snapshot.yaml:/directus/snapshot.yaml:ro
    environment:
      KEY: ${KEY:?KEY required}
      SECRET: ${SECRET:?SECRET required}
      DB_CLIENT: sqlite3
      DB_FILENAME: /directus/database/data.db
      ADMIN_EMAIL: ${ADMIN_EMAIL:?ADMIN_EMAIL required}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:?ADMIN_PASSWORD required}
      ADMIN_TOKEN: ${ADMIN_TOKEN:?ADMIN_TOKEN required}
      PUBLIC_URL: ${PUBLIC_URL:-http://localhost:8056}
      CORS_ENABLED: "true"
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost:3000}

  nuxt:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      # Browser-facing URL — must be reachable from the user's browser.
      NUXT_PUBLIC_DIRECTUS_URL: ${NUXT_PUBLIC_DIRECTUS_URL:-http://localhost:8056}
      # SSR-only URL — used inside the container network.
      NUXT_DIRECTUS_INTERNAL_URL: http://directus:8056
      NUXT_DIRECTUS_TOKEN: ${ADMIN_TOKEN}
    depends_on:
      - directus
